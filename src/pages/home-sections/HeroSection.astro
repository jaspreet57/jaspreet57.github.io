---
import HeroProfileImage from "@components/HeroProfileImage.astro";
---

<style lang="scss">
  .hero-section {
    @apply relative /* position */
        px-4 sm:px-8 md:px-20 /* content padding */
        max-w-7xl /* width */
        mx-auto xs:mt-6 sm:mt-10 md:mt-20 lg:mt-12 /* margins top */
        xs:mb-2 sm:mb-2 md:mb-20 lg:mb-20 /* margins bottom */
        flex flex-col md:justify-center md:items-center /* flex center */
        md:flex-row-reverse /* flex directions */
        /* end apply */;
  }

  .hero-image {
    @apply md:basis-2/5 /* flex size */
        xs:px-10 sm:px-20 md:px-0 /* content padding */
        /* end apply */;
  }

  .hero-content {
    @apply md:basis-3/5 /* flex size */
        lg:pt-14 /* paddings */
        /* end apply */;
    z-index: 10;
  }

  .grayscale {
    filter: url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg'><filter id='grayscale'><feColorMatrix type='matrix' values='0.3333 0.3333 0.3333 0 0 0.3333 0.3333 0.3333 0 0 0.3333 0.3333 0.3333 0 0 0 0 0 1 0'/></filter></svg>#grayscale"); /* Firefox 3.5+ */
    filter: gray; /* IE6-9 */
    -webkit-filter: grayscale(100%); /* Chrome 19+ & Safari 6+ */
  }

  .grayscale:hover {
    filter: none;
    -webkit-filter: grayscale(0%);
  }

  .scroll-to-next {
    @apply absolute /* positions */
        md:-bottom-24 lg:-bottom-28 /* margins */
        /* end apply */;
  }

  .custom-gradient-text {
    @apply bg-clip-text bg-gradient-to-r from-violet-600 to-secondary-default text-transparent;
  }
</style>

<!-- Hero Section -->
<section class="hero-section">
  <div class="hero-image">
    <HeroProfileImage />
  </div>
  <div class="hero-content">
    <header>
      <span class="lg:text-2xl font-normal"> Hi, my name is </span>
      <h1
        class="block text-5xl/normal sm:text-6xl/normal lg:text-7xl/normal font-bold font-lato text-secondary-default primary-selected"
      >
        Jaspreet Singh
      </h1>
      <h2
        class="block tracking-wider text-xl/normal sm:text-2xl/normal lg:text-3xl/normal md:my-1 font-medium"
      >
        I'm a Full Stack Developer.
      </h2>
      <p
        class="font-bold font-mono mb-4 mt-4 inline-block primary-selected custom-gradient-text lg:whitespace-nowrap"
      >
        Expert in ReactJS, NodeJS, and AWS Cloud Solutions.
      </p>
      <p>
        With a strong focus on building and designing web apps, I have gained
        experience across a wide range of projects using Javascript. Over the
        past few years, I have been dedicated to developing AWS cloud solutions
        specifically tailored for IOT devices and applications.
      </p>
      <a
        class="mt-6 inline-flex justify-center items-center gap-x-3 text-center bg-gradient-to-tl from-blue-600 to-violet-600 hover:from-violet-600 hover:to-blue-600 border border-transparent text-white text-sm font-medium rounded-md focus:outline-none focus:ring-2 focus:ring-blue-600 focus:ring-offset-2 focus:ring-offset-white py-3 px-4 dark:focus:ring-offset-gray-800"
        href="#"
      >
        Contact Me
        <svg
          class="w-3 h-3"
          width="16"
          height="16"
          viewBox="0 0 16 16"
          fill="none"
        >
          <path
            d="M5.27921 2L10.9257 7.64645C11.1209 7.84171 11.1209 8.15829 10.9257 8.35355L5.27921 14"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"></path>
        </svg>
      </a>
    </header>
  </div>
</section>
<!-- End Hero Section -->
