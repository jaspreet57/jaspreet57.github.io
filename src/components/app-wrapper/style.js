import styled from 'styled-components';

const StyledWrapper = styled.div`
  position: relative;
`;

const AnotherStyle = styled.div`
    color: black;
`;

export {
    StyledWrapper,
    AnotherStyle,
}