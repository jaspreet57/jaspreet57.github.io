{
  "name": "jaspreet57.github.io",
  "version": "1.0.0",
  "description": "Profile of Jaspreet Singh (erjaspreet57@gmail.com)",
  "main": "index.html",
  "scripts": {
    "start": "rm -rf dist && parcel index.html",
    "build": "rm -rf dist && parcel build index.html",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist -b master"
  },
  "author": "Jaspreet Singh",
  "license": "MIT",
  "devDependencies": {
    "gh-pages": "^2.0.1",
    "parcel-bundler": "^1.12.0",
    "parcel-plugin-static-files-copy": "^2.0.0",
    "sass": "^1.17.2"
  },
  "dependencies": {
    "css": "^2.2.4",
    "jquery": "^3.3.1"
  }
}
